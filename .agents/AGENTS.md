# Workspace Project Rules

- **Git Branching Strategy**: Whenever any code or content update is made, ALWAYS push to the `test` branch first (`git push origin test`). NEVER push to `main` unless the user explicitly commands: "push to main".
